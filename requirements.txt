torchvision==0.17.2
transformers==4.41.1
streamlit==1.35.0
matplotlib
numpy

# PyTorch CPUビルドを探す URL（必ず独立行に）
--find-links https://download.pytorch.org/whl/cpu/torch_stable.html

# CPU専用の torch（PyPI 上で存在する最後の +cpu ビルド）
torch==2.7.1+cpu
